---
config:
  POSTGRES_DB:       notejam
  POSTGRES_USER:     notejam

secrets:
  POSTGRES_PASSWORD: xxxxxxxxxxxxxxxx

resources:
  requests:
    cpu:             150m
    memory:          256Mi
  limits:
    cpu:             450m
    memory:          1Gi
